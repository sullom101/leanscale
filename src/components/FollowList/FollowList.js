import React from "react";
import PropTypes from "prop-types";
import styles from "./follow-list.module.scss";

function FollowList(props) {
  return (
    <div>
      <p>Follow List</p>
    </div>
  );
}

FollowList.propTypes = {};

export default FollowList;
